services:
  - type: web
    name: polling-server
    env: node
    rootDir: server
    plan: free
    region: oregon
    buildCommand: npm install
    startCommand: node index.js
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: CLIENT_ORIGINS
        sync: false

